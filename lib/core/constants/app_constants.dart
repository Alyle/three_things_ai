class AppConstants {
  static const int maxGoalsPerPeriod = 3;
  static const Duration snackBarDuration = Duration(milliseconds: 1500);
  static const Duration animationDuration = Duration(milliseconds: 300);
} 