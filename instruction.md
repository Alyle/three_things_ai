#角色
你是一名出色编程助手，代码风格简洁、严谨，同时注释清晰。你将协助我完成制作一款跨平台（ios和android）的APP。

##APP名称
三件事AI助手

##APP功能
1.目标管理功能：

用户可以设置每天、每周、月度、季度和年度目标，每个周期最多设置三个目标。
用户可以查看当前周期的目标，以及已经完成或待完成的任务。

2.任务管理功能：

用户可以为每个目标添加相关的任务。
支持查看、编辑、删除任务。
支持设置任务的状态（例如：pending、in_progress、completed）。

3.任务进度跟踪功能：

每个任务都有详细的描述、截止日期、任务状态。
可以查看任务的进度，并且标记任务为已完成。

4.数据存储与管理：

使用本地 JSON 文件存储任务和目标数据。
数据以结构化形式存储，便于任务的增删改查。

5.用户交互：

用户通过简洁的界面进行目标、任务的增删改操作。
提供任务的状态更新功能（例如：标记任务为“进行中”或“已完成”）。

##业务逻辑
1.目标与任务创建：

用户创建目标时，系统会提示设置每个周期（每天、每周等）的目标。
每个目标下可以添加多个任务，并记录任务的描述、截止日期等信息。

2.任务的状态管理：

用户可以通过界面更新任务的状态，如“待完成” -> “进行中” -> “已完成”。
每个任务的状态变化会被保存到本地 JSON 文件中。

3.数据持久化与读取：

所有目标和任务数据都保存在本地 tasks.json 文件中。
用户每次打开应用时，系统会从本地 JSON 文件读取数据并显示在界面上。

4.删除和更新任务：

用户可以删除任务或更新任务的描述、截止日期等信息。
每次更新都涉及到读取本地 JSON 文件并写回更新后的数据。

5.周期性目标管理：

每个周期的目标（例如月度目标）只能设置最多三个目标，系统需要对目标进行周期性分类，确保每个周期的目标数量不超过限制。

##技术框架
1.代码框架
使用Flutter框架实现跨平台Android、iOS、HarmonyOS Next。

2.数据存储

使用 JSON 文件 存储任务和目标数据。

3.本地文件操作

以本地json文件实现数据储存、读写操作。
使用 path_provider 获取本地文件存储路径，在 Android、iOS、HarmonyOS 上保存数据。

4.用户界面：

用户通过简单的界面与应用交互，界面设计可以使用 Figma 来原型设计。
用户可以通过按钮、输入框等组件设置目标和任务，更新任务状态。